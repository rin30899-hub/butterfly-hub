<!doctype html>
<html>
<head>
  <meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1">
  <title><%= title %> – <%= siteTitle %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">
  <style>
    .tool-btn.active { box-shadow: 0 0 0 .2rem rgba(25,135,84,.25); border-color:#198754; }
    .swatch { width:28px;height:28px;border-radius:50%; border:1px solid rgba(0,0,0,.12); cursor:pointer; }
    .swatch.active { outline: 3px solid #198754; }
    .preview-dot { width:28px;height:28px;border-radius:50%; border:2px solid #212529; background:#156f47; }
    .paper {
      background:
        linear-gradient(180deg, rgba(25,135,84,.06), rgba(25,135,84,.02));
      border:1px solid rgba(0,0,0,.08);
      border-radius: .75rem;
      padding: 1rem;
    }
    .canvas-wrap {
      position: relative;
      overflow: hidden;
      border-radius: .5rem;
      box-shadow: 0 8px 24px rgba(0,0,0,.06);
    }
    #pad { display:block; background:#fff; }
    /* grid overlay is drawn in JS; this is just for feel */
  </style>
</head>
<body>
  <%- include('partials/navbar') %>

  <main class="container py-4">
    <div class="row g-3">
      <!-- Left: tools -->
      <div class="col-lg-4 col-xl-3">
        <div class="paper">
          <h1 class="h5 mb-3">Butterfly Sketch Studio</h1>

          <label class="form-label">Title</label>
          <div class="input-group mb-3">
            <span class="input-group-text"><i class="bi bi-card-text"></i></span>
            <input id="title" class="form-control" placeholder="Drawing title">
          </div>

          <div class="row g-2 mb-3">
            <div class="col-6 d-grid">
              <button id="pencil" class="btn btn-outline-secondary tool-btn" data-bs-toggle="tooltip" title="Pencil (P)">
                <i class="bi bi-pencil"></i> Pencil
              </button>
            </div>
            <div class="col-6 d-grid">
              <button id="eraser" class="btn btn-outline-secondary tool-btn" data-bs-toggle="tooltip" title="Eraser (E)">
                <i class="bi bi-eraser"></i> Eraser
              </button>
            </div>
          </div>

          <div class="mb-3">
            <label class="form-label d-flex justify-content-between">
              <span>Brush size</span>
              <span><code id="sizeLabel">6 px</code></span>
            </label>
            <input id="size" type="range" min="1" max="40" value="6" class="form-range">
            <div class="d-flex align-items-center gap-2 mt-2">
              <div class="preview-dot" id="previewDot"></div>
              <input id="color" type="color" value="#156f47" class="form-control form-control-color" title="Pick color">
            </div>
          </div>

          <div class="mb-3">
            <div class="d-flex flex-wrap gap-2">
              <!-- quick swatches -->
              <div class="swatch" data-color="#212529" style="background:#212529"></div>
              <div class="swatch" data-color="#156f47" style="background:#156f47"></div>
              <div class="swatch" data-color="#0d6efd" style="background:#0d6efd"></div>
              <div class="swatch" data-color="#dc3545" style="background:#dc3545"></div>
              <div class="swatch" data-color="#ffc107" style="background:#ffc107"></div>
              <div class="swatch" data-color="#fd7e14" style="background:#fd7e14"></div>
              <div class="swatch" data-color="#6f42c1" style="background:#6f42c1"></div>
            </div>
          </div>

          <div class="row g-2 mb-3">
            <div class="col-6 d-grid">
              <button id="undo" class="btn btn-light" data-bs-toggle="tooltip" title="Undo (Ctrl/⌘+Z)">
                <i class="bi bi-arrow-counterclockwise"></i> Undo
              </button>
            </div>
            <div class="col-6 d-grid">
              <button id="redo" class="btn btn-light" data-bs-toggle="tooltip" title="Redo (Ctrl/⌘+Y)">
                <i class="bi bi-arrow-clockwise"></i> Redo
              </button>
            </div>
          </div>

          <div class="row g-2 mb-3">
            <div class="col-6 form-check">
              <input class="form-check-input" type="checkbox" id="grid">
              <label class="form-check-label" for="grid"><i class="bi bi-grid"></i> Show grid</label>
            </div>
            <div class="col-6 d-grid">
              <button id="clear" class="btn btn-outline-danger" data-bs-toggle="tooltip" title="Clear canvas">
                <i class="bi bi-x-circle"></i> Clear
              </button>
            </div>
          </div>

          <div class="d-grid gap-2">
            <button id="save" class="btn btn-success">
              <i class="bi bi-cloud-arrow-up"></i> Save
            </button>
            <button id="export" class="btn btn-outline-success">
              <i class="bi bi-file-earmark-image"></i> Export PNG
            </button>
          </div>
        </div>
      </div>

      <!-- Right: canvas -->
      <div class="col-lg-8 col-xl-9">
        <div class="canvas-wrap">
          <canvas id="pad" width="1000" height="600"></canvas>
        </div>

        <!-- Help / Instructions -->
        <div class="mt-3">
          <div class="card">
            <div class="card-body">
              <h2 class="h6 mb-3"><i class="bi bi-info-circle"></i> Tips & Instructions</h2>
              <ul class="mb-2">
                <li>Use <strong>Pencil</strong> to draw and <strong>Eraser</strong> to correct.</li>
                <li>Pick a color from the swatches or the color picker; adjust <strong>Brush size</strong> as needed.</li>
                <li>Toggle <strong>Show grid</strong> for alignment.</li>
                <li><strong>Undo/Redo</strong> supports multiple steps.</li>
                <li><strong>Save</strong> stores a JSON representation on the server. <strong>Export PNG</strong> downloads an image locally.</li>
                <li>Open <strong>Gallery</strong> to view, load back into Studio, or delete saved drawings.</li>
              </ul>
              <div class="small text-muted">
                Shortcuts: <kbd>P</kbd> Pencil, <kbd>E</kbd> Eraser, <kbd>Ctrl/⌘ + Z</kbd> Undo, <kbd>Ctrl/⌘ + Y</kbd> Redo.
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Toasts -->
    <div class="toast-container position-fixed bottom-0 end-0 p-3">
      <div id="toast" class="toast text-bg-success" role="alert" aria-live="assertive" aria-atomic="true">
        <div class="d-flex">
          <div class="toast-body">Saved!</div>
          <button type="button" class="btn-close btn-close-white me-2 m-auto" data-bs-dismiss="toast"></button>
        </div>
      </div>
    </div>
  </main>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <script src="/js/studio.js"></script>
</body>
</html>
